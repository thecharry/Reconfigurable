# RC-Designer 启动说明

## 每次使用

1. 在 MATLAB 中将“当前文件夹”切换到本项目根目录。
2. 在命令窗口输入 `Run_RCDesigner` 并回车。
3. 如需修改界面，可用 App Designer 打开 `app/RCDesignerPrototype.mlapp`。日常运行请使用 `Run_RCDesigner`，它会自动使用 MLAPP 中最新保存的代码。

## 当前软件功能

- 输入条件：可查看和修改任务场景、飞行器质量/转动惯量、控制器参数、推力器参数、故障状态和优化设计参数。
- 参数文件：在顶部工具栏或“输入条件”页点击“加载参数文件 / 保存参数文件”，支持 `txt`、`csv`、`xls`、`xlsx`，文件中会同时保存任务参数和当前推力器布局。
- 优化计算：点击“开始优化（快速）”调用 `Optim_Algorithm.m`，默认使用快速演示参数；正式计算时可手动调大种群规模和迭代次数。
- 布局设计：可编辑推力器安装位置和方向，点击“应用并刷新布局”后更新三维视图并检查安装边界、最小间距约束。
- 可重构判断：针对指定故障推力器编号和故障台数，调用 `Reconfig_eval.m` 判断剩余系统是否可重构。
- 可重构设计：调用 `Reconfig_eval.m` 输出控制能力 Jc、可诊断性 Jo、控制指令 Jt、能量消耗 Jf 和综合指标，并在表格/柱状图中显示。
- 调用策略：调用 `Thruster_invocation.m` 生成轨控脉宽、姿控脉宽和最终复用脉宽。
- 闭环仿真：调用 `Closedloop_sim.m`，根据界面中的仿真时长和故障时间绘制位置误差、姿态误差曲线。
- 结果导出：可在“结果报告”页导出 MAT/CSV 数据或生成文本报告。

## 推荐操作流程

1. 打开软件后先在“输入条件”页确认默认参数；如已有参数文件，点击“加载参数文件”。
2. 如需重新优化，进入“优化计算”页点击“开始优化（快速）”；若用于正式结果，可把种群规模和迭代次数调大。
3. 在“布局设计”页检查或修改推力器表格，点击“应用并刷新布局”。
4. 在“可重构判断”页输入故障推力器编号和故障台数，点击“进行可重构判断”。
5. 在“可重构设计”页点击“计算可重构设计指标”。
6. 在“调用策略”页点击“生成调用策略”。
7. 在“闭环仿真”页设置仿真时长、故障时间，点击“运行闭环仿真”。
8. 在“结果报告”页导出数据或报告文本。

## 在 App Designer 中打开

当前版本包含完整的 App Designer 设计模型。设计视图中可以看到全部页面、网格布局和控件，并可在右侧“组件浏览器”中选择和修改；代码视图中可以修改回调函数与算法调用。点击“运行”按钮即可启动完整软件。

正常情况下不会再出现“组件发生内部错误”的警告。如果以后再次出现类似警告，请点击“取消”，不要点击“仍要打开”，然后在项目根目录运行 `build_rcdesigner_mlapp` 重新生成。

## 文件位置

- 运行版：`app/RCDesignerPrototype.mlapp`
- 固定启动器：`Run_RCDesigner.m`
- 界面源码：`source/RCDesignerPrototype.m`
- 重新生成运行版：`build_rcdesigner_mlapp.m`

运行版和源码已放在不同目录中，避免 MATLAB 把同名 `.m` 与 `.mlapp` 混淆。以 `.mlapp` 为主要编辑文件；App Designer 保存后，`Run_RCDesigner` 会自动同步最新代码再运行。不要把 `source` 文件夹长期添加到 MATLAB 搜索路径。

## 本次修复后的首次使用

旧版本已经被当前 MATLAB 进程缓存，因此请完整退出 MATLAB 后重新打开一次。以后按上述方式启动即可。
